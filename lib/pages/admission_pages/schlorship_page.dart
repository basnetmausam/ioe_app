import 'package:flutter/material.dart';

class SchlorshipPage extends StatelessWidget {
  const SchlorshipPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
