import 'package:flutter/material.dart';

class CapabilitiesPage extends StatelessWidget {
  const CapabilitiesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
