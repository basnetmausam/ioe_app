import 'package:flutter/material.dart';

class IndustryWorkPage extends StatelessWidget {
  const IndustryWorkPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
